# Contributing

欢迎提交 issue 和 pull request。这个项目的目标是保持简单、可本地运行、不会引入不必要的账号风险。

## 开发

```bash
npm start
```

提交前建议运行：

```bash
npm run check
```

## 约定

- 不提交真实 Cookie、账号信息或抓包数据
- 保持零运行时依赖，除非新增依赖能明显降低维护成本
- 修改网易云接口相关逻辑时，请在 PR 中说明测试过的接口和失败场景
- UI 文案保持中文，代码标识保持英文
